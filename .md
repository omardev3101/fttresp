Quero todas as respostas seja em portugues-BR
quero que você sempre use o VSCode para editar arquivos
quero que quando for para editar qualquer coisa você me mande um passo a passo do que vai ser feito para eu aprovar antes de editar.
quero que quando for para editar qualquer coisa você me mande um resumo das coisas que foram feitas para eu aprovar antes de editar.
quero que seja tudo bem organizado em pastas e arquivos.
sempre que salvar novas versoes no github, utilize o esquema com nome da alteração + data(dd-mm-aaaa) + horario do commit (hh-mm-ss) + versão(v001) ex: nome_da_alteracao_20012026_200000_v001

https://github.com/omardev3101/fttresp.git

Utilize sempre o deploy_dev.ps1 e suas credenciais para acessar o vps, sempre usa o plink para conectar no vps via terminal (windows).
Para o deploy utilize sempre a opção "p" (push) do script.

para acessar o vps use o plink com os seguintes comandos:

plink -ssh root@[IP_DO_VPS] -pw [SENHA] "Credenciais do vps no .env USERVPS,
PASSWDVPS

o sistema deve rodar tanto localhost, como no vps (ubuntu).

todos os arquivos devem estar comentados de forma clara e objetiva, explicando o que cada arquivo faz e como ele funciona.  

Os arquivos nao podem ultrapassar a 1000 linhas de codigo

Todos os arquivos de software devem seguir o padrao do projeto, que é::

padrao_software:
---

    ---

o codigo deve ser Modular e organizado, os arquivos nao podem ultrapassar a 1000 linhas de codigo

Sempre utilize o arquivo .editorconfig para formatar o codigo.  

O css deve ser responsivo e utilizar media queries para se adaptar a diferentes tamanhos de tela, utilize o arquivo style.css para definir os estilos globais.  

sempre utilize o arquivo .gitignore para ignorar arquivos desnecessarios, utilize o arquivo .gitignore_global para ignorar arquivos desnecessarios em todos os projetos. 

todas as chaves devem ficar no .env, exitem duas chaves, uma para localhost e outra para vps, ex: LOCALHOST="localhost" VPS="vps" sempre que for acessar o .env utilize o metodo .env.localhost ou .env.vps dependendo do ambiente que esta sendo utilizado

O .env nunca pode ser compartilhado ou enviado para o github        

O .env deve ser sempre comentado de forma clara e objetiva, explicando o que cada chave faz e como ela funciona.  


---
---
o banco de dados deve ser schemafull, ou seja, deve ter um schema padrão para todas as tabelas, e cada tabela deve ter um schema para cada ambiente, ex: schema_localhost, schema_vps

sempre utilize o .env para definir as chaves, ex: LOCALHOST="localhost" VPS="vps" sempre que for acessar o .env utilize o metodo .env.localhost ou .env.vps dependendo do ambiente que esta sendo utilizado

O .env nunca pode ser compartilhado ou enviado para o github 